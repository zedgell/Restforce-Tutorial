<%= form_with(model: firstname, local: true) do |form| %>
  <% if firstname.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(firstname.errors.count, "error") %> prohibited this firstname from being saved:</h2>

      <ul>
      <% firstname.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :lastname %>
    <%= form.text_field :lastname %>
  </div>

  <div class="field">
    <%= form.label :company %>
    <%= form.text_field :company %>
  </div>

  <div class="field">
    <%= form.label :email %>
    <%= form.text_field :email %>
  </div>

  <div class="field">
    <%= form.label :phone %>
    <%= form.text_field :phone %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
